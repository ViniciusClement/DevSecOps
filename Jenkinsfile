pipeline {

    agent any

    stages {
        stage ("Inicial"){
            steps {
                echo 'iniciando a pipeline'
            }
            steps {
                git-bash.exe 
            }
        }
    }
}
