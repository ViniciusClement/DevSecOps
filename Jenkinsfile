pipeline {

    agent any

    stages {
        stage ("Inicial"){
            steps {
                echo 'iniciando a pipeline'
            }

            stepts {
                python3 --version 
            }
        }
    }
}
