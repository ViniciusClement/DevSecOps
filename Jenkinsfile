pipeline {

    agent any

    stages {
        stage ("Inicial"){
            steps {
                echo 'iniciando a pipeline'
            }
            steps {
                "C:\Program Files\Git\git-bash.exe" --cd-to-home
            }
        }
    }
}
