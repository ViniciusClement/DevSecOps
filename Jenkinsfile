pipeline {

    agent any

    stages {
        stage ("Inicial"){
            steps {
                echo 'iniciando a pipeline'
            }

            steps {
                bat 'echo %PATH%'
            }
        }
    }
}
